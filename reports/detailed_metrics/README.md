# Подробные отчеты по классам

В этом каталоге приложение сохраняет:

- JSON-отчеты с поклассовыми метриками для `validation` и `test`;
- CSV-файлы с матрицами ошибок для `validation`;
- CSV-файлы с матрицами ошибок для `test`.
