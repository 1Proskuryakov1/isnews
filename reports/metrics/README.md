# Отчеты по метрикам

В этом каталоге приложение сохраняет JSON-отчеты с метриками качества моделей:

- `Accuracy`
- `Precision`
- `Recall`
- `F1-score`

Отчеты формируются отдельно для `train`, `validation` и `test`.
