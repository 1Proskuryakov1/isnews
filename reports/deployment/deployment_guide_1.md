# Инструкция по развёртыванию isnews

- Целевой сценарий: `streamlit_local_or_cloud`.
- JSON-манифест: `C:\projects\isnews\reports\deployment\deployment_manifest_2.json`.

## Проверка артефактов

- `main.py`: найден (file).
- `requirements.txt`: найден (file).
- `src/isnews`: найден (directory).
- `models`: найден (directory).
- `reports`: найден (directory).

## Шаги развёртывания

1. Создать и активировать чистое виртуальное окружение Python 3.11+.
2. Установить зависимости командой `pip install -r requirements.txt`.
3. Убедиться, что в репозитории присутствуют каталоги `models`, `reports` и `data`.
4. Запустить приложение командой `python main.py`.
5. Открыть локальный адрес Streamlit в браузере и проверить загрузку интерфейса.
6. Проверить сценарий deployment `streamlit_local_or_cloud` на целевой машине или в облаке.

Сохранённые модели не обнаружены, перед демонстрацией потребуется обучить и сохранить хотя бы одну модель.

## Замечания

- В репозитории пока не найдено сохранённых моделей для демонстрации после развёртывания.
- Базовые файлы запуска присутствуют: main.py, requirements.txt и пакет src/isnews.
