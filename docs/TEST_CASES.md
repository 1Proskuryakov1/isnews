# Тест-кейсы проекта IsNews

## TC-01. Запуск приложения

| Поле | Значение |
| --- | --- |
| Цель | Проверить запуск GUI через корневой `main.py`. |
| Предусловия | Установлены зависимости из `requirements.txt`. |
| Шаги | Выполнить `python main.py`. |
| Ожидаемый результат | Запускается Streamlit-приложение без ошибок импорта. |
| Статус | Пройдено. |

## TC-02. Создание демонстрационных артефактов

| Поле | Значение |
| --- | --- |
| Цель | Проверить воспроизводимое обучение моделей. |
| Предусловия | Проект открыт в корне репозитория. |
| Шаги | Выполнить `python scripts\train_demo_artifacts.py`. |
| Ожидаемый результат | Создаются датасет, split-файлы, TF-IDF признаки, `model.joblib`, `model1.joblib`, `model_vectorizer.joblib` и итоговые отчеты. |
| Статус | Пройдено. |

## TC-03. Проверка структуры обучающего датасета

| Поле | Значение |
| --- | --- |
| Цель | Убедиться, что сохраненный датасет пригоден для обучения. |
| Предусловия | Есть `data/raw/news_demo_dataset.csv`. |
| Шаги | Открыть CSV и проверить колонки. |
| Ожидаемый результат | В датасете есть колонки `text` и `label`, 180 строк и 5 классов. |
| Статус | Пройдено. |

## TC-04. Проверка разбиения датасета

| Поле | Значение |
| --- | --- |
| Цель | Проверить наличие train, validation и test-выборок. |
| Предусловия | Выполнен скрипт обучения. |
| Шаги | Проверить каталог `data/splits/news_demo_dataset/`. |
| Ожидаемый результат | Существуют `train.csv`, `validation.csv`, `test.csv`; размеры: 126, 27, 27 строк. |
| Статус | Пройдено. |

## TC-05. Загрузка основной модели

| Поле | Значение |
| --- | --- |
| Цель | Проверить, что `model.joblib` является рабочей моделью. |
| Предусловия | Есть `models/classifiers/model.joblib` и `models/vectorizers/model_vectorizer.joblib`. |
| Шаги | Загрузить модель и векторизатор через `joblib.load`. |
| Ожидаемый результат | Загружается `LogisticRegression`, число классов равно 5. |
| Статус | Пройдено. |

## TC-06. Загрузка дополнительной модели

| Поле | Значение |
| --- | --- |
| Цель | Проверить, что `model1.joblib` является рабочей моделью. |
| Предусловия | Есть `models/classifiers/model1.joblib`. |
| Шаги | Загрузить модель через `joblib.load`. |
| Ожидаемый результат | Загружается `MultinomialNB`, число классов равно 5. |
| Статус | Пройдено. |

## TC-07. Проверка Accuracy на test-выборке

| Поле | Значение |
| --- | --- |
| Цель | Подтвердить выполнение требования Accuracy >= 70%. |
| Предусловия | Есть сохраненные модели, векторизатор и `test.csv`. |
| Шаги | Выполнить `python scripts\verify_project.py`. |
| Ожидаемый результат | Accuracy у `model.joblib` и `model1.joblib` не ниже 0.70. |
| Фактический результат | Accuracy обеих моделей равна 1.0 на сохраненной test-выборке. |
| Статус | Пройдено. |

## TC-08. Проверка notebook-файлов

| Поле | Значение |
| --- | --- |
| Цель | Убедиться, что `.ipynb` файлы можно открыть в Google Colab/Jupyter. |
| Предусловия | Есть каталог `notebooks/`. |
| Шаги | Проверить JSON-структуру notebook-файлов. |
| Ожидаемый результат | Все notebook-файлы имеют формат `nbformat = 4` и содержат список ячеек. |
| Статус | Пройдено. |

## TC-09. Проверка закрепления зависимостей

| Поле | Значение |
| --- | --- |
| Цель | Проверить воспроизводимость окружения. |
| Предусловия | Есть `requirements.txt`. |
| Шаги | Проверить, что каждая зависимость указана с `==`. |
| Ожидаемый результат | Все версии библиотек закреплены. |
| Статус | Пройдено. |

## TC-10. Проверка объема кода

| Поле | Значение |
| --- | --- |
| Цель | Проверить требование о 2000 логических строках кода. |
| Предусловия | Есть исходный код в `src/` и `scripts/`. |
| Шаги | Выполнить `python scripts\verify_project.py`. |
| Ожидаемый результат | Количество логических строк Python-кода больше 2000. |
| Статус | Пройдено. |
